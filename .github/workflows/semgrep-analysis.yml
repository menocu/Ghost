name: Semgrep
on: [pull_request]
env:
  GITHUB_TOKEN: fd02e2f6dc8dae7d97487438d22e7316ffbc7293
  
jobs:
  semgrep:
    runs-on: ubuntu-latest
    name: Check
    steps:
      - uses: actions/checkout@v1
      - name: Semgrep
        id: semgrep
        uses: returntocorp/semgrep-action@v1
        with:
          config: p/r2c
